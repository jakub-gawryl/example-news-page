import '../styles/globals.css'
import type { AppProps } from 'next/app'

const ExampleNewsPage = ({ Component, pageProps }: AppProps) => <Component {...pageProps} />;

export default ExampleNewsPage;
